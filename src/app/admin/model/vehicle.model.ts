export interface Vehicle{
    id?: string,
    model: string,
    brand_id: string,
    category_id: string,
    year: number,
    price: number,
    description?: string,
    images?: any,
    deleting?: any,
}
